config {
  type: "view",
  schema: "reporting",
  materialized: true,
  bigquery: {
    /*partitionBy: "date(created_at)",*/
    clusterBy: ["type"]
  }
}

SELECT *
FROM ${ref("stg_posts_answers")}
WHERE title like '%bash%'